# Ansible

# ansible --version
#     Based on the file path of the ansible.cfg
#     Create an inventory file within the same directory

# touch inventory.txt

# Create IOSXE network devices, and add CSR1 & CSR 2 to the inventory File

# Create an SNMP playbook to push SNMP configs to both CSR's
#     snmp-server community ansible-test RO 
#     snmp-server community location Bragg
#     snmp-server contact NOT_ANSIBLE

